add "cancel" command

window control:
add close all to close all windows
maximise, minimise, restore <app name>



add shutdown and restart commands (gui only)
remove static word placements & replace with dynamic
only enable camera for login and enrolment, key input MUST use camera auth

on the model settings panel, only log conversations when in conversation mode & LLM is loaded but always log commands
move cmd/conv pill to replace "MODEL:none"
add a "node settings" panel (slide down from top)

popup speech commands window "Help/show commands"

kids mode with passphrase protected exit - child safe models only

automatically assign tasks to seperate small models with dedicated prompts (load model on demand):
coding -
research assistant - research_assistant_prompt.txt
context - prompt_context.txt
composing documents -
image generation -
sound generation -

etc..

/!\ all apps should be pygame based and take from the already active pygame setup including fonts, imgaes etc..

app template for easy generation of new apps
default wake word: "computer", pause media when detected, add text field in settings for wake word..

volume commands:
volume up/down
set volume 50

husarnet vpn connection for wwm (world-wide mesh) like tailscale-vpn as seen by Rob Braxman YT